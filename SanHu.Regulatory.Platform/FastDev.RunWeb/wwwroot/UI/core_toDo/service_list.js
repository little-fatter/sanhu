function server(page){    page.bind('beforeShowList', function (e)    {        var page = e.page, gridOptions = e.options;        var column = pbc.web.helper.first(gridOptions.columns, function (a) { return a.name == "Title"; });        if (column == null) return;         column.render = function (r)        {            return '<a href="javascript:void()" data-link="' + r.Link + '" class="todolink">' + r.Title + '</a>';        };         gridOptions.onAfterShowData = function ()        {            $("a.todolink", this.element).click(function ()            {                                 var link = $(this).attr("data-link");                 top.openTab({                    text: "代办任务",                    url: link,                    tabid: 'core_todo_form',                    data: {                        callback: function ()                        {                            page.reload();                        }                    }                });             });        };     } );      }